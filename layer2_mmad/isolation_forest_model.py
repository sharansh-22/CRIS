"""
isolation_forest_model.py — Isolation Forest Anomaly Detection

Uses an Isolation Forest to flag microstructure anomalies across
multiple features (OFI, VPIN, spread, message rate).
"""


def train_isolation_forest(features) -> None:
    """Fit an Isolation Forest on historical feature vectors."""
    pass


def predict_anomalies(model, features) -> list:
    """Score new observations and return anomaly labels."""
    pass
